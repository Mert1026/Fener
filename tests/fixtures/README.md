# Connector fixtures

Small public response excerpts retrieved 2026-08-31 for offline contract regression only. These are test data and are never seeded into the application database.

models.dev: MIT, models.dev contributors. LiteLLM: MIT, Berri AI. OpenRouter: public models and GPT-4o endpoint responses. See `docs/sources.md` for endpoint and terms references. LLM Stats fixture is the documented catalog response example; it has not been authenticated/live-verified and does not imply benchmark support.
