# Development

Requires Python 3.12+, uv, Node 22+, pnpm 11 and Docker Compose for PostgreSQL. Start Docker before infrastructure. SQLite can run offline tests but does not replace PostgreSQL integration checks.
